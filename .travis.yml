services:

language: python

python:
  - "3.7"

addons:
  apt:
    packages:
    - x11-utils
    - libxkbcommon-x11-0
    - python3-pyqt5.qtsql
    - python3-pytestqt

services: 
  - xvfb
  - mysql

dist: bionic

env:
  global:
   - API_PATH=docs/source/api

cache: 
 - directories:
   - $HOME/.cache/pip

install: 
  - pip install -U pip
  - pip install nibabel pandas wonambi bidso
  - pip install codecov 
  - pip install -e .

script:
  - pytest --cov=xelo2 tests/ -vv

after_success:
  - codecov

notifications:
  email: false
