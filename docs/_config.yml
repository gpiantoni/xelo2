name: xelo2 (MySQL-to-BIDS approach)
description: xelo2 is a SQL database with a Python GUI to organize EEG, ECoG and fMRI data
remote_theme: pages-themes/tactile@v0.2.0
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
